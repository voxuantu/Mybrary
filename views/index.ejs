<h2 class="page-header">Recently Added</h2>
<div>
    <% books.forEach(book =>{ %>
        <a href="/books/<%= book.id%>">
            <img src="<%= book.coverImagePath%>" width="150px" height="200px"></img>
        </a>
    <% })%>
</div>